{
  "name": "infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "deploy": "npm run build && cdk deploy --all",
    "destroy": "cdk destroy --all"
  },
  "devDependencies": {
    "aws-cdk-lib": "^2.219.0",
    "constructs": "10.3.0",
    "ts-node": "10.9.2",
    "typescript": "5.6.3",
    "source-map-support": "^0.5.21"
  }
}
